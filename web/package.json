{
  "name": "lgtm-webview",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Builds the vendored, fully-offline JS bundles for LGTM's editor window (tree + diff panes).",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "@pierre/diffs": "1.2.11",
    "@pierre/trees": "1.0.0-beta.4"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
